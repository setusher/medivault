# LLM
CHAT_API_URL=https://openrouter.ai/api/v1/chat/completions
CHAT_API_KEY=YOUR_API_KEY_HERE
PRIMARY_MODEL=mistralai/mistral-small-3.2-24b-instruct:free
FALLBACK_MODEL=deepseek/deepseek-r1-0528:free
LLM_MAX_TOKENS=1600
LLM_THROTTLE_MS=0

# Optional subset for week runs
WEEK_RANGE=1-1

# Firebase (local dev only – do NOT commit real keys)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_USERS_COLLECTION=users
GOOGLE_APPLICATION_CREDENTIALS=secrets/firebase-sa.json

# Where to deliver outputs (dev API or firebase)
OUTPUT_TARGET=dev_api
DEV_BASE_URL=http://localhost:8000

SIGNING_SECRET=replace-with-random-string
